<?xml version="1.0"?>
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" name="StellarWP Changelog Embed" xsi:noNamespaceSchemaLocation="vendor/squizlabs/php_codesniffer/phpcs.xsd">
	<description>The custom ruleset for the StellarWP Changelog Embed plugin.</description>

	<!-- For help in understanding this file: https://github.com/squizlabs/PHP_CodeSniffer/wiki/Annotated-ruleset.xml -->
	<!-- For help in using PHPCS: https://github.com/squizlabs/PHP_CodeSniffer/wiki/Usage -->

	<!-- What to scan -->
	<file>.</file>
	<!-- Ignoring Files and Folders:
		https://github.com/squizlabs/PHP_CodeSniffer/wiki/Advanced-Usage#ignoring-files-and-folders -->
	<exclude-pattern>vendor/</exclude-pattern>
	<exclude-pattern>vendor-prefixed/</exclude-pattern>
	<exclude-pattern>tests/_data</exclude-pattern>
	<exclude-pattern>build/modules/blocks/**/*.asset.php</exclude-pattern>
	<exclude-pattern>node_modules/</exclude-pattern>

	<!-- How to scan -->
	<!-- Formats of the report -->
	<arg name="report" value="full,summary,source"/>
	<!-- Show results with colors -->
	<arg name="colors"/>
	<!-- Enables cache -->
	<arg name="cache"/>
	<!-- Enables parallel processing when available for faster results. -->
	<arg name="parallel" value="50"/>
	<!-- Limit to PHP files -->
	<arg name="extensions" value="php"/>

	<!-- For help in understanding this custom sniff properties:
		https://github.com/WordPress-Coding-Standards/WordPress-Coding-Standards -->
	<rule ref="WordPress">
		<!-- Ignoring YODA rule -->
		<exclude name="WordPress.PHP.YodaConditions.NotYoda"/>
		<!-- Ignore disallow short array syntax -->
		<exclude name="Universal.Arrays.DisallowShortArraySyntax"/>
	</rule>

	<rule ref="WordPress.Files.FileName">
		<exclude name="WordPress.Files.FileName.NotHyphenatedLowercase"/>
	</rule>

	<rule ref="WordPress.Files.FileName.InvalidClassFileName">
		<exclude-pattern>src/ChangelogEmbed/*</exclude-pattern>
	</rule>

	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="stellar_changelog_embed"/>
				<element value="stellarwp"/>
			</property>
		</properties>
	</rule>

	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="stellar-changelog-embed"/>
			</property>
		</properties>
	</rule>

	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<exclude-pattern>src/views/*</exclude-pattern>
	</rule>

	<rule ref="SlevomatCodingStandard.TypeHints.ReturnTypeHintSpacing"/>
</ruleset>
